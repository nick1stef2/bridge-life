export default function SectionTitle({ title }) {

  return (

    <h1
      style={{
        color: "#22c55e",
        marginBottom: "25px",
        fontSize: "36px",
      }}
    >
      {title}
    </h1>

  )
}